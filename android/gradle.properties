android.useAndroidX=true
android.enableJetifier=true
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official

# OTA: repo de GitHub donde se publican los releases, en formato usuario/repo. La app pregunta por
# https://github.com/<githubRepo>/releases/latest/download/update.json — una URL que NO cambia al
# publicar una versión nueva. Mientras esto no se rellene, la app no comprueba actualizaciones.
# githubRepo=usuario/animeav1
