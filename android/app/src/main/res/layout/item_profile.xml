<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="150dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:paddingTop="12dp"
    android:paddingBottom="12dp"
    android:layout_marginEnd="16dp"
    android:focusable="true"
    android:focusableInTouchMode="false"
    android:background="@drawable/card_focus_bg">

    <!-- Avatar: inicial sobre color. Sin imágenes — en una TV se lee de lejos y no hay ficheros
         que gestionar ni que meter en la copia de seguridad. -->
    <TextView
        android:id="@+id/profile_avatar"
        android:layout_width="72dp"
        android:layout_height="72dp"
        android:gravity="center"
        android:textColor="#FFFFFF"
        android:textSize="30sp"
        android:textStyle="bold"
        android:background="@drawable/profile_avatar_bg" />

    <TextView
        android:id="@+id/profile_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:gravity="center"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        android:singleLine="true"
        android:ellipsize="end" />

    <!-- ⚠️ `invisible`, NUNCA `gone`. Con `gone` la tarjeta del perfil activo medía 54px más que las
         demás (medido en captura: 554 frente a 500), así que la fila salía desigual: el rótulo tiene
         que ocupar su sitio siempre y solo aparecer y desaparecer. El adapter alterna entre VISIBLE
         e INVISIBLE, no GONE. -->
    <TextView
        android:id="@+id/profile_active"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/profile_active"
        android:textColor="@color/accent_light"
        android:textSize="10sp"
        android:letterSpacing="0.1"
        android:visibility="invisible" />
</LinearLayout>
